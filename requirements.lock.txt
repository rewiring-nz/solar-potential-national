# Exact environment of the machine that BUILDS THE PUBLISHED MAPS.
#
# Frozen from the GCP VM's venv on 31 Aug 2026, the same venv that produced
# the current Queenstown and Island Bay layouts. requirements.txt states the
# loose intent (">=") for a fresh install; THIS file is the reproducible set.
#
# Why it exists (code review, 31 Aug): requirements.txt pinned nothing, so
# "the environment" was whatever pip resolved on the day, remembered by hand.
# That already cost a run when matplotlib turned out to be missing on the VM.
# It is also a correctness risk, not just a build risk: this tool publishes
# kWh figures, and a pvlib or shapely release can move them with no commit in
# our history to explain the change.
#
# Known drift at the time of writing: the Mac dev venv had scipy 1.18.0 while
# the VM had 1.18.1. Patch-level, but the build machine and the test machine
# should not differ at all -- align the Mac to this file.
#
# Rebuild an environment from here:
#   python -m venv .venv && .venv/bin/pip install -r requirements.lock.txt

affine==3.0.1
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
geopandas==1.1.4
h5py==3.16.0
idna==3.19
kiwisolver==1.5.1
laspy==2.7.0
lazrs==0.8.2
matplotlib==3.11.1
numpy==2.5.2
opencv-python-headless==5.0.0.93
packaging==26.3
pandas==3.0.5
pillow==12.3.0
pvlib==0.15.2
py-spy==0.4.2
pyogrio==0.13.0
pyparsing==3.3.2
pyproj==3.7.2
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
pytz==2026.3.post1
rasterio==1.5.1
requests==2.34.2
scipy==1.18.1
shapely==2.1.2
six==1.17.0
urllib3==2.7.0
